# After editing this gitattributes file, you will need to re-normalize all files.
# git rm --cached -r .  (Remove everything from the index)
# git reset --hard      (Write both the index and working directory from git's database)
# git add .             (Prepare to make a commit by staging all the files that will get normalized)
# git commit -m "Normalize line endings"

*           text=auto whitespace=trailing-space,space-before-tab,-indent-with-non-tab,tab-in-indent,tabwidth=4

# Readme
*.md        text

# Visual Studio
*.cs        text diff=csharp
*.csproj    text
*.sln       text